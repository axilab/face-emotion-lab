services:
  face-emotion-lab:
    build:
      context: .
      args:
        NEXT_PUBLIC_BASE_PATH: /face-emotion-lab
    ports:
      - "3100:3000"
    env_file:
      - .env.local
    restart: unless-stopped
